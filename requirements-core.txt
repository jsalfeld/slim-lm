# Minimal core dependencies for Apertus post-training
# Use this if you have issues with the full requirements.txt

# Core Deep Learning (required)
torch>=2.1.0
transformers>=4.36.0
accelerate>=0.25.0
peft>=0.7.0
bitsandbytes>=0.41.0

# Training Libraries (required)
trl>=0.7.10
datasets>=2.14.0
evaluate>=0.4.0

# Data Processing (required)
pandas>=2.0.0
numpy>=1.24.0
scikit-learn>=1.3.0
jsonlines>=4.0.0

# Utilities (required)
scipy>=1.11.0
sentencepiece>=0.1.99
protobuf>=3.20.0

# Optional monitoring (uncomment if you want visualization)
# wandb>=0.16.0
# tensorboard>=2.15.0
