# Core Deep Learning
torch>=2.1.0
transformers>=4.36.0
accelerate>=0.25.0
peft>=0.7.0
bitsandbytes>=0.41.0

# Training Libraries
trl>=0.7.10
datasets>=2.14.0
evaluate>=0.4.0

# Optimization & Memory (Optional - uncomment if you have CUDA and want speed boost)
# flash-attn>=2.5.0  # Requires CUDA, build tools, and compatible GPU
# triton>=2.1.0  # GPU optimization library

# Data Processing
pandas>=2.0.0
numpy>=1.24.0
scikit-learn>=1.3.0

# Utilities
scipy>=1.11.0
sentencepiece>=0.1.99
protobuf>=3.20.0

# Optional monitoring (uncomment if needed)
# wandb>=0.16.0  # Weights & Biases tracking
# tensorboard>=2.15.0  # TensorBoard visualization

# Optional: Fast training (comment out if issues)
# unsloth @ git+https://github.com/unslothai/unsloth.git

# JSONL handling
jsonlines>=4.0.0
